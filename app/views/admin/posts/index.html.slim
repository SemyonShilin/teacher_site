h1 Управление постами
.row.margin-bottom-30
  .col-md-12
    ul.nav.nav-pills
      li
        = link_to new_admin_post_path do
          | Новый Пост
      li
        a href="#"
          | Всего Постов
          span.badge = @posts.count
.row.margin-bottom-30
  .col-md-12
    h4.margin-bottom-15 Таблица всех записей
    table.table.table-striped.table-hover.table-bordered
      thead
        tr
          th №
          th Заголовок
          th Время создания
          th.text-right Действия
      tbody
        - @posts.each do |post|
          tr
            td = post.id
            td = post.title.truncate_words(10)
            td =  t('.posted', date: l(post.updated_at, format: :date))
            td
              .btn-group
                button.btn.btn-info.dropdown-toggle data-toggle="dropdown" type="button"
                  | Действия
                  span.caret
                ul.dropdown-menu role="menu"
                  li
                    = link_to 'Просмотр', [:admin, post]
                  li
                    = link_to 'Редактировать', [:edit, :admin, post]
                  li
                    = link_to 'Удалить', [:admin, post], method: :delete
